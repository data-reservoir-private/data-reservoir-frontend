export type ITransjakartaResponse = {
  'corridor': {
    code: string
    category: string
    color: string
    name: string
    image: string,
    isDeleted: boolean,
    effectiveDate: string,
    pictureEffectiveDate: string,
  }
}