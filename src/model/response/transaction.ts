export interface TransactionMonthlyResponse {
  year: number;
  month: number;
  total: number;
}

export interface TransactionCategoryResponse {
  year: number;
  month: number;
  total: number;
  category: string;
}